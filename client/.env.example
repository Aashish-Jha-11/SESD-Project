# Frontend environment variables
# Leave VITE_API_BASE_URL empty in local dev to use Vite proxy (/api -> http://localhost:3001).
VITE_API_BASE_URL=

# Socket server URL (usually your backend origin, without /api).
# Local default is http://localhost:3001 if this is empty.
VITE_SOCKET_URL=
